/*
import Firebase from 'firebase';

const getUsersRequest = () => {
  const fb = new Firebase('https://beezer-test-fb095.firebaseio.com/');
  fb.child('user/123').once('value', function(userSnap) {
    fb.child('media/123').once('value', function(mediaSnap) {
      // extend function: https://gist.github.com/katowulf/6598238
      console.log(userSnap, mediaSnap);
    });
  });
};

export default getUsersRequest;*/
